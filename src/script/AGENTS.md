# Script Module — Agent Knowledge Base

**Scope**: `src/script/` | **Score**: 17 (21 files, Bitcoin Script + extensions)

---

## Overview

Bitcoin Script interpreter, descriptor system, and signature operations. Handles transaction validation via script execution. Contains miniscript implementation for spending policy composition.

---

## Structure

```
src/script/
├── interpreter.cpp, interpreter.h    # Script execution engine (88KB)
├── descriptor.cpp, descriptor.h      # Output descriptors
├── miniscript.h, miniscript.cpp      # Miniscript spending policies
├── script.cpp, script.h               # Script primitives
├── sigcache.cpp, sigcache.h           # Signature caching
├── bitcoinconsensus.cpp, .h           # Consensus script validation
├── script_error.cpp, script_error.h   # Error handling
└── keyorigin.h                        # Key origin tracking
```

---

## Where to Look

| Task | File | Notes |
|------|------|-------|
| **Script execution** | `interpreter.cpp` | 88KB, core interpreter |
| **Descriptors** | `descriptor.cpp` | 93KB, output descriptors |
| **Miniscript** | `miniscript.h` | 137KB header, spending policies |
| **Signature cache** | `sigcache.cpp` | Validation caching |
| **Consensus** | `bitcoinconsensus.cpp` | Consensus rules |

---

## Key Symbols

| Symbol | Type | Role |
|--------|------|------|
| `CScript` | class | Bitcoin Script container |
| `Interpreter` | class | Script execution engine |
| `Descriptor` | class | Output descriptor parsing |
| `Miniscript` | class | Spending policy composition |
| `SignatureHash` | function | SigHash computation |

---

## PoS Considerations

**OP_RETURN Staking Support**:
- Blackcoin More supports OP_RETURN in stake outputs
- Segment Leader feature allows metadata in staking transactions
- Not blocked like in Bitcoin Core

**Script Validation**:
- Standard Bitcoin Script semantics (no PoS-specific changes)
- All standard opcodes supported
- SegWit validation active (80% threshold)

---

## ⚠️ CMake Migration (29.x → 30.x)

**When migrating script module to CMake**:
- Preserve descriptor compilation
- Keep miniscript implementation
- Maintain sigcache architecture

---

## Anti-Patterns (THIS MODULE)

- **NEVER**: Modify consensus-critical script semantics without full testing
- **NEVER**: Remove OP_RETURN support (used in staking)
- **NEVER**: Change signature hash algorithm

---

*Generated by /init-deep*