# Qt GUI Module — Agent Knowledge Base

**Scope**: `src/qt/` | **Score**: 15 (119 files, GUI layer)

---

## ⚠️ CRITICAL: Bitcoin 26.x → 30.x Upgrade Context

**Current Phase**: See .sisyphus/plans/bitcoin-28.4.0-merge-plan.md v2.7

### UI Considerations for Upgrades

| Component | Bitcoin Core | Blackcoin More | Action |
|-----------|--------------|----------------|--------|
| Fee selector | Dynamic estimation | Static (100k sat/kvB) | Remove estimation UI |
| RBF checkbox | Enabled by default | Disabled | Remove RBF UI |
| Wallet migration | Mandatory | Optional | Make migration voluntary |
| SegWit UI | Active | 80% threshold | Update activation status |

### Fee Display (Static Structure)

```cpp
// src/qt/coincontroldialog.cpp
// Blackcoin More uses static fees - no fee estimation UI needed
static const CAmount MIN_FEE = 100000;  // Fixed, not dynamic
```

**UI Impact**:
- No fee slider or estimation dropdown
- Fixed 100,000 sat/kvB displayed
- No "economical" / "conservative" fee options

### RBF UI (Completely Disabled)

```cpp
// src/qt/sendcoinsdialog.cpp
// RBF checkbox does not exist - functionality is completely disabled
// No "Enable Replace-By-Fee" option in settings
```

### Wallet Migration UI

- Migration is OPTIONAL, not mandatory
- "Migrate Wallet" button available but not required
- BDB wallets continue to work without migration

### Anti-Patterns (THIS MODULE)

- **NEVER**: Modify visuals directly (delegate to frontend-ui-ux-engineer)
- **NEVER**: Use `QDialog::exec()` (use `QDialog::show()`)
- **NEVER**: Skip CAmount validation
- **TODO**: `sendcoinsdialog.cpp` — Replace `QDialog::exec()`
- **NEVER**: Add dynamic fee estimation UI
- **NEVER**: Add RBF checkbox or UI elements
- **NEVER**: Force wallet migration UI

---

## Overview

Qt5 GUI implementation for blackmore-qt. Wallet GUI, transaction sending, network status. 119 files including forms, locales, resources. UI changes MUST delegate to frontend-ui-ux-engineer.

---

## Structure

```
src/qt/
├── bitcoin.cpp, bitcoin.h    # GUI entry point
├── bitcoinamountfield.cpp    # Amount input widget
├── sendcoinsdialog.cpp       # Send payment UI
├── walletframe.cpp           # Wallet tab container
├── walletview.cpp            # Wallet navigation
├── addressbookpage.cpp       # Address book
├── transactionview.cpp       # Tx history
├── locale/                   # Translations (125 locales)
├── res/                      # Icons, animations, fonts
└── forms/                    # Qt Designer .ui files
```

---

## Where to Look

| Task | File | Notes |
|------|------|-------|
| **Entry** | `bitcoin.cpp` | main() for GUI |
| **Send** | `sendcoinsdialog.cpp` | Payment UI |
| **Amount** | `bitcoinamountfield.cpp` | CAmount handling quirk |
| **Wallet** | `walletview.cpp` | Main wallet interface |

---

## ⚠️ CMake Migration (29.x → 30.x)

**Bitcoin Core 30.x migrated from Autotools to CMake.**

**When migrating Qt module to CMake**:
- Preserve static fee display (no fee estimation UI)
- Remove RBF checkbox/UI elements
- Preserve BDB wallet support in Qt
- Never enable dynamic fee estimation UI

**Reference**: `agent/CMake_MIGRATION.md` for complete build system migration details.

---

## Anti-Patterns (THIS MODULE)

- **NEVER**: Modify visuals directly (delegate to frontend-ui-ux-engineer)
- **NEVER**: Use `QDialog::exec()` (use `QDialog::show()`)
- **NEVER**: Skip CAmount validation
- **TODO**: `sendcoinsdialog.cpp` — Replace `QDialog::exec()`

---

## UI Changes (VISUAL ONLY)

For color, layout, typography, animation, responsiveness, hover states, shadows, borders, icons → **DELEGATE** to `frontend-ui-ux-engineer`.

For logic (API calls, data fetching, state management, event handlers) → handle directly.

---

*Generated by /init-deep*
