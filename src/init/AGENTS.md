# Init Module — Agent Knowledge Base

**Scope**: `src/init/` | **Score**: 14 (initialization layer)

---

## ⚠️ CRITICAL: Bitcoin 26.x → 30.x Upgrade Context

**Current Phase**: See .sisyphus/plans/bitcoin-28.4.0-merge-plan.md v2.7

### Never-Port Features (Init Module)

| Bitcoin Core Feature | Blackcoin More Status | Action |
|---------------------|----------------------|--------|
| RBF default enable | Completely disabled | Never enable in init |
| Fee estimation init | Not initialized | Skip fee estimation setup |
| BDB wallet creation | Deprecated | Preserve BDB initialization |

### Initialization Sequence (Blackcoin-Specific)

```cpp
// src/init/common.cpp - Common initialization
// src/init/bitcoin-gui.cpp - GUI initialization
// src/init/bitcoin-node.cpp - Node/daemon initialization
// src/init/bitcoin-wallet.cpp - Wallet tool initialization

// CRITICAL: These files handle:
// 1. BDB wallet initialization (required for Blackcoin More)
// 2. Static fee setup (100,000 sat/kvB)
// 3. PoS kernel initialization
// 4. RBF policy disable
```

### Anti-Patterns (THIS MODULE)

- **NEVER**: Enable RBF policy initialization
- **NEVER**: Initialize fee estimation (static fees)
- **NEVER**: Skip BDB initialization (required for wallet)
- **NEVER**: Remove GetAdjustedTime() initialization

---

## Overview

Application initialization layer. Handles startup sequence for daemon, GUI, wallet tool. Separated from main() to enable multi-process architecture. Contains context setup, logging, parameter validation.

---

## Structure

```
src/init/
├── common.cpp, common.h      # Shared initialization code
├── bitcoin-gui.cpp           # GUI (blackmore-qt) init
├── bitcoin-node.cpp          # Daemon (blackmored) init
├── bitcoin-qt.cpp            # Qt-specific init
└── bitcoin-wallet.cpp       # Wallet tool init
```

---

## Where to Look

| Task | File | Notes |
|------|------|-------|
| **Common init** | `common.cpp` | Shared initialization |
| **Daemon init** | `bitcoin-node.cpp` | blackmored startup |
| **GUI init** | `bitcoin-gui.cpp` | blackmore-qt startup |
| **Wallet init** | `bitcoin-wallet.cpp` | blackmore-wallet startup |
| **Main init** | `src/init.cpp` | Core initialization |

---

## Key Symbols

| Symbol | Type | Role |
|--------|------|------|
| `InitCommon` | namespace | Shared init functions |
| `BitcoinGuiInit` | function | GUI initialization |
| `BitcoinNodeInit` | function | Node initialization |
| `BitcoinWalletInit` | function | Wallet tool init |

---

## Initialization Sequence

```cpp
// 1. Parameter parsing (src/init.cpp)
// 2. Logging setup (src/init/common.cpp)
// 3. Network initialization
// 4. BDB/SQlite wallet initialization
// 5. Chainstate initialization
// 6. PoS kernel setup (Blackcoin-specific)
// 7. RPC server startup
```

---

## Blackcoin-Specific Initialization

### Static Fee Setup

```cpp
// No fee estimation initialization
// Fees are static: 100,000 sat/kvB
// Unlike Bitcoin Core, no CBlockPolicyEstimator initialization
```

### RBF Policy (Disabled)

```cpp
// RBF mempool policy is disabled
// No opt-in RBF signaling
// First-seen rule remains active
```

### BDB Wallet (Required)

```cpp
// BDB 6.2 initialization required
// Unlike Bitcoin Core 30.x which removed BDB
// Both BDB and SQLite wallet backends supported
```

---

## ⚠️ CMake Migration (29.x → 30.x)

**When migrating init module to CMake**:
- Preserve BDB initialization logic
- Skip fee estimation initialization
- Keep RBF disabled
- Preserve GetAdjustedTime() setup

**Reference**: `agent/CMake_MIGRATION.md` for build system migration details.

---

*Generated by /init-deep*