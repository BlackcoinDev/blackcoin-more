# Policy Module — Agent Knowledge Base

**Scope**: `src/policy/` | **Score**: 14 (12 files, mempool policy)

---

## ⚠️ CRITICAL: Bitcoin 26.x → 30.x Upgrade Context

**Current Phase**: See .sisyphus/plans/bitcoin-28.4.0-merge-plan.md v2.7

### Never-Port Features (Policy Module)

| Bitcoin Core Feature | Blackcoin More Status | Action |
|---------------------|----------------------|--------|
| RBF mempool policy | **DISABLED** | Never enable `mempoolreplacement` |
| Dynamic fee estimation | **Static 100k sat/kvB** | Never port fee estimation |
| Fee rate filtering | Static only | Never implement smart fees |

### Static Fee Constants

```cpp
// src/policy/feerate.h
static const CAmount DEFAULT_MIN_RELAY_TX_FEE = 100000;  // 100k sat/kvB (FIXED)

// src/policy/fees_args.cpp
// Fee estimation is NOT initialized - Blackcoin uses static fees
```

**Key Difference**: Bitcoin Core has dynamic fee estimation based on mempool. Blackcoin More uses a **FIXED** minimum relay fee of 100,000 sat/kvB.

### RBF Policy (Permanently Disabled)

```cpp
// src/policy/rbf.h - NOT PRESENT
// RBF is completely disabled in Blackcoin More
// No mempool replacement policy exists

// src/kernel/mempool_options.h
bool full_rbf{DEFAULT_MEMPOOL_FULL_RBF};  // DEFAULT_MEMPOOL_FULL_RBF = false
```

---

## Overview

Mempool acceptance policies: fee rates, RBF (disabled), and transaction replacement. Blackcoin uses static fees with NO RBF support.

---

## Structure

```
src/policy/
├── feerate.cpp, feerate.h       # Fee rate definitions (STATIC)
├── fees_args.cpp, fees_args.h   # Fee estimation args (unused)
├── packages.cpp, packages.h     # Transaction package validation
├── policy.cpp, policy.h         # Policy constants
├── rbf.cpp, rbf.h               # RBF policy (DISABLED in Blackcoin)
└── settings.cpp, settings.h     # mempool settings
```

---

## Where to Look

| Task | File | Notes |
|------|------|-------|
| **Fee rates** | `feerate.h` | Static 100k sat/kvB |
| **RBF check** | `rbf.h` | DISABLED - returns false |
| **Package validation** | `packages.cpp` | Ancestor/descendant limits |
| **Policy constants** | `policy.h` | Script policy flags |

---

## Key Symbols

| Symbol | Type | Role |
|--------|------|------|
| `CFeeRate` | class | Fee rate (sat/kvB) |
| `DEFAULT_MIN_RELAY_TX_FEE` | const | 100,000 sat/kvB (FIXED) |
| `IsRBFOptIn` | function | Always returns false (RBF disabled) |

---

## Static Fee Structure

**Blackcoin More**:
- Minimum relay fee: **100,000 sat/kvB** (fixed)
- No fee estimation
- No dynamic adjustment
- No economical/conservative fee options

**Bitcoin Core** (DO NOT PORT):
- `CBlockPolicyEstimator` fee estimation
- FeeRate bump for RBF
- Smart fee rate filtering
- `estimatesmartfee` RPC

---

## Anti-Patterns (THIS MODULE)

- **NEVER**: Enable RBF policy (`mempoolreplacement=0`)
- **NEVER**: Implement fee estimation
- **NEVER**: Add `bumpfee` RPC support
- **NEVER**: Change static fee constants without consensus awareness

---

## ⚠️ CMake Migration (29.x → 30.x)

**When migrating policy module to CMake**:
- Preserve static fees (no fee estimation)
- Keep RBF disabled in configuration
- Never enable dynamic fee policies

**Reference**: `agent/CMake_MIGRATION.md` for build system migration details.

---

*Generated by /init-deep*