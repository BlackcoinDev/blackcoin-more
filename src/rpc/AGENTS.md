# RPC Module — Agent Knowledge Base

**Scope**: `src/rpc/` | **Score**: 17 (30 files, API layer)

---

## ⚠️ CRITICAL: Bitcoin 26.x → 30.x Upgrade Context

**Current Phase**: See .sisyphus/plans/bitcoin-28.4.0-merge-plan.md v2.7

### Never-Port RPC Features

| Bitcoin Core RPC | Blackcoin More Status | Action |
|-----------------|----------------------|--------|
| `estimateSmartFee` | Not implemented | Never port fee estimation |
| `estimatepriority` | Not implemented | Never port priority estimation |
| `bumpfee` | Not implemented | Never port RBF bumping |
| `sendrawtransaction` (RBF) | No RBF support | Never enable RBF signaling |

### PoS-Specific RPCs (MUST PRESERVE)

```cpp
// src/rpc/mining.cpp
// getstakinginfo - PoS staking information
// getnetworkhashps - Network hashrate (PoW-compatible)
// getblocktemplate - Block template generation (PoS-aware)
```

### Fee-Related RPCs (Static Structure)

**Available**:
- `getmempoolinfo` - Mempool status
- `getrawmempool` - Raw mempool contents
- `getrawtransaction` - Transaction details

**NOT Available**:
- ❌ `estimateSmartFee` - Dynamic fee estimation
- ❌ `estimatepriority` - Dynamic priority estimation

**Static Fee Structure**:
```cpp
// 100,000 sat/kvB minimum relay fee (fixed)
const CAmount DEFAULT_MIN_RELAY_TX_FEE = 100000;
```

### RBF-Related RPCs

**⚠️ RBF is COMPLETELY DISABLED in Blackcoin More**

The following Bitcoin Core RPCs do NOT exist in Blackcoin More:
- `bumpfee` - RBF fee bumping
- `walletcreatefundedpsbt` (RBF options) - RBF funding

### Anti-Patterns (THIS MODULE)

- **NEVER**: Use deprecated RPCs without `-deprecatedrpc=`
- **NEVER**: Skip parameter validation in handlers
- **NEVER**: Block in RPC handlers (use async)
- **NEVER**: Implement dynamic fee estimation RPCs
- **NEVER**: Implement RBF-related RPCs
- **NEVER**: Remove PoS-specific RPCs (`getstakinginfo`, etc.)

---

## Overview

JSON-RPC server implementation. Exposes blockchain, wallet, mining, and network operations via HTTP/JSON. High centrality — many files depend on RPC handlers.

---

## Structure

```
src/rpc/
├── server.cpp, server.h      # RPC dispatcher
├── blockchain.cpp            # Chain queries (129KB)
├── rawtransaction.cpp        # Tx operations (93KB)
├── net.cpp                   # Network info (54KB)
├── mempool.cpp               # Mempool queries (49KB)
├── mining.cpp                # PoS mining (45KB)
├── client.cpp                # RPC client bindings
├── server_util.cpp           # Server utilities
└── protocol.h                # RPC protocol types
```

---

## Where to Look

| Task | File | Notes |
|------|------|-------|
| **Dispatcher** | `server.cpp` | RPC command routing |
| **Blockchain** | `blockchain.cpp` | getblock, getchaininfo |
| **Raw tx** | `rawtransaction.cpp` | createrawtransaction |
| **Mining** | `mining.cpp` | getstakinginfo |
| **Registration** | `register.h` | RPC command macros |

---

## Key Symbols

| Symbol | Type | Role |
|--------|------|------|
| `RPCServer` | class | HTTP server |
| `CRPCTable` | class | Command table |
| `RPCCommand` | struct | Command metadata |

---

## Conventions

- **Naming**: `RPCHelpMan` for documentation
- **Error codes**: JSON-RPC standard (-32600 to -32000)
- **Deprecation**: `-deprecatedrpc=` flag for old methods

---

## ⚠️ CMake Migration (29.x → 30.x)

**Bitcoin Core 30.x migrated from Autotools to CMake.**

**When migrating RPC module to CMake**:
- Preserve RBF DISABLED in RPC responses
- Preserve static fee display (100,000 sat/kvB)
- Never port estimateSmartFee or fee estimation RPCs
- Preserve GetAdjustedTime() usage in mining RPCs

**Reference**: `agent/CMake_MIGRATION.md` for complete build system migration details.

---

## Anti-Patterns (THIS MODULE)

- **NEVER**: Use deprecated RPCs without `-deprecatedrpc=`
- **NEVER**: Skip parameter validation in handlers
- **NEVER**: Block in RPC handlers (use async)

---

## Deprecated RPCs

| Deprecated | Alternative | Flag |
|------------|-------------|------|
| `getinfo` | Combined calls | N/A |
| `generate` | `generatetoaddress` | `-deprecatedrpc=generate` |
| `signrawtransaction` | `signrawtransactionwithwallet` | N/A |

---

*Generated by /init-deep*
